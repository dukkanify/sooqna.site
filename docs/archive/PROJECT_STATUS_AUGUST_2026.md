# تقرير حالة مشروع سوقنا (Sooqna)

**التاريخ:** 11 أغسطس 2026  
**المرجع:** فرع `main` · الإنتاج `https://sooqna.site`  
**الحزمة:** `sooqna-web@0.1.0-beta`  
**آخر commit على main:** `7c3f9c5` — إخفاء عناصر غير عملية في صفحة إضافة الإعلان

---

## 1) ملخص تنفيذي

سوقنا منصة إعلانات عربية RTL للإمارات، مبنية بـ **Next.js 16 + React 19 + TypeScript + Tailwind 4**. الموقع الحي يعمل على **sooqna.site** (HTTP 200).

المشروع في مرحلة **Closed Beta**: واجهة كاملة تقريبًا مع بيانات تجريبية (mocks / localStorage / in-memory stores)، ولوحة إدارة، ومسار دفع Stripe عند ضبط المفاتيح. **لا توجد قاعدة بيانات دائمة على `main`** بعد — البيانات ليست نظام سجل إنتاج متعدد الأجهزة.

| المؤشر | القيمة |
|--------|--------|
| صفحات App Router | **48** |
| صفحات الإدارة | **19** |
| مسارات API | **~68** |
| اختبارات آلية | غير مُعدّة |
| حالة الإنتاج | حي على sooqna.site |

---

## 2) ماذا تم إنجازه

### 2.1 الأساس التقني والهوية
- إعادة تسمية العلامة إلى **سوقنا / Sooqna** وربط النطاق **sooqna.site**
- تصميم عربي RTL، ثيم فاتح/داكن، نظام أزرار ذهبي موحّد
- تحسينات أداء للصفحة الرئيسية والبحث
- إصلاحات hydration، تباين النصوص على الهيرو، وأيقونات الوضع الليلي

### 2.2 واجهة السوق (Marketplace)
| المسار | الحالة |
|--------|--------|
| `/` الصفحة الرئيسية | مكتمل (هيرو، بحث، تصنيفات، مميزة، قريبة، ضمان، تطبيق، فوتر) |
| `/categories` و `/categories/[slug]` | مكتمل |
| `/search` | مكتمل (فلاتر، typeahead، سجل بحث) |
| `/featured` | مكتمل |
| `/listings/[slug]` | مكتمل (معرض، تفاصيل، ضمان، تواصل، شريط موبايل) |
| `/listings/new` | مكتمل (معالج 3 خطوات + حقول حسب التصنيف + **بحث الماركة**) |
| تعديل إعلان محلي/كتالوج | مكتمل |

**أحدث إنجازات إضافة الإعلان:**
- اختيار ماركة قابل للبحث (سيارات / موبايلات / إلكترونيات)
- إخفاء زر «بحث» العائم وزر «أضف إعلانك» داخل صفحة الإضافة نفسها

### 2.3 الحساب والمصادقة
- `/login` · `/register` · `/forgot-password` · `/verify-email` · `/complete-account`
- حسابات تجريبية `@sooqna.demo` (بما فيها أدمن)
- جلسة عبر كوكي `sooqna_session`
- إصلاحات أمنية حديثة:
  - منع تسريب كلمة المرور في رابط GET ([PR #86](https://github.com/dukkanify/UAE-Sales/pull/86))
  - تحصين قراءة كوكي الأدمن عند الترميز المزدوج ([PR #87](https://github.com/dukkanify/UAE-Sales/pull/87))

### 2.4 التجارة والثقة
| الميزة | الحالة على main |
|--------|-----------------|
| `/checkout` معالج الدفع | واجهة مكتملة + Stripe عند ضبط المفاتيح |
| `/checkout/success` | موجود |
| `/orders` · `/orders/[id]` · `/order-status` | موجود (مخازن محلية/ذاكرة) |
| `/wallet` | واجهة محفظة تجريبية |
| `/escrow` | واجهة ضمان مالي تجريبية |
| `/chat` | محادثات تجريبية (localStorage) |

### 2.5 لوحة الإدارة `/admin`
**19 صفحة** تشمل: نظرة عامة، مستخدمين، إعلانات، طلبات، ضمان، محافظ، Stripe، نزاعات، تصنيفات، تقارير، إعدادات، تدقيق، حجوزات معاينة، طلبات عروض، طلبات وظائف، مفضلات، عناوين، إشعارات، تحليلات.

- محمية عبر `proxy.ts`: يتطلب دور `admin` في كوكي الجلسة
- البيانات مخازن in-process (ليست Postgres على main)

### 2.6 جودة وتجربة مستخدم (آخر فترة)
- الوضع الليلي على الهيدر/الهيرو/البطاقات
- تباين أيقونات البطاقات في الوضع الداكن
- إصلاح زر تطبيق الفلاتر المقطوع في البحث
- جلسة Frontend QA مع لقطات تحت `artifacts/qa/` (فرع التقرير)

---

## 3) ما هو جزئي / تجريبي

| المجال | الواقع |
|--------|--------|
| الدفع | Stripe جاهز تقنيًا؛ يعتمد على env؛ mock خارج الإنتاج |
| المحفظة / الضمان | واجهات وledger تجريبي — ليست بنية بنكية |
| المصادقة OTP بالبريد | أعلام وبنية موجودة؛ الديمو OTP ما زال محور البيتا |
| المفضلة / الحجوزات / عروض الأسعار | واجهات + APIs تشغيلية على مخازن |
| الوثائق | بعضها قديم (يقول إن admin/checkout غير موجودين) |
| فرع Backend/Prisma | موجود على فروع جانبية — **غير مدمج كمعمارية main** |

---

## 4) المهام المتبقية (حسب الأولوية)

### P0 — استقرار الإنتاج والثقة
1. **تحديث الوثائق** (`AGENTS.md`, `README`, `KNOWN_LIMITATIONS`, `PROJECT_STATUS_REPORT`) لتطابق الواقع
2. **مراجعة أمن الجلسة للأدمن** (كوكي JSON قابل للتلاعب في نموذج الديمو — غير كافٍ لعمليات حقيقية)
3. **ضبط env على sooqna.site**: Stripe، webhook، `NEXT_PUBLIC_APP_URL`، نطاق الكوكي
4. **إعادة بناء Cloud Agent snapshot** (فشل install قديم بسبب `prisma generate` على شجرة stale)

### P1 — إكمال الصفحات الناقصة
5. بناء **`/support`** (مركز دعم حقيقي بدل Coming Soon)
6. بناء **`/disputes/new`** (نموذج نزاع مرتبط بالطلب/الضمان)
7. إكمال تدفقات الضيف في الدفع إن بقيت فجوات UX

### P2 — Backend حقيقي
8. دمج طبقة بيانات دائمة (Postgres/Prisma أو API منفصل) بدل mocks/localStorage
9. مزامنة الإعلانات/الطلبات/المحادثات عبر الأجهزة
10. تخزين صور سحابي (بدل الاعتماد الكامل على Unsplash/روابط ثابتة)
11. مصادقة إنتاجية (OTP بريد حقيقي / إلغاء الاعتماد على كلمات ديمو)

### P3 — منتج ونمو
12. UAE PASS (العلم موجود، التكامل غير مفعّل)
13. تطبيق جوال أو PWA كامل فوق نفس الـ API
14. لوحة تحليلات إنتاجية متصلة ببيانات حقيقية
15. **Suite اختبارات آلية** (Playwright smoke + lint/CI إلزامي)

### P4 — تحسينات UX معروفة من QA
16. معالجة بطاقات بصور رمادية/فاشلة على بعض أقسام الرئيسية
17. توحيد رسائل «قريبًا» في الفوتر/التنقل حتى لا توعد بميزات غير جاهزة

---

## 5) خريطة الحالة السريعة

```
مكتمل ●●●●●●●●○○  واجهة السوق + إضافة إعلان + بحث
مكتمل ●●●●●●●○○○  مصادقة ديمو + ملف شخصي
مكتمل ●●●●●●●●○○  لوحة إدارة (UI)
جزئي  ●●●●●○○○○○  دفع Stripe / طلبات
جزئي  ●●●●○○○○○○  محفظة / ضمان / شات
ناقص ●●○○○○○○○○  Backend دائم + اختبارات
ناقص ●○○○○○○○○○  Support / Disputes حقيقيان
ناقص ○○○○○○○○○○  UAE PASS / تطبيق أصلي
```

---

## 6) مخاطر يجب الانتباه لها

1. **خلط الفروع:** بعض بيئات الـ Agent قد تكون على فرع Prisma قديم — الحالة الرسمية للمنتج هي `main` + sooqna.site  
2. **هشاشة البيانات:** إعادة تشغيل السيرفر أو تبديل الجهاز يفقد بيانات localStorage/الذاكرة  
3. **أمن الأدمن الديمو:** مناسب للبيتا فقط  
4. **توثيق قديم:** قد يضلّل أصحاب المصلحة حول checkout/admin  
5. **فشل install في Cloud:** رسالة `prisma: not found` من لقطة قديمة — ليست عطلًا في `sooqna-web` الحالي  

---

## 7) توصية المرحلة القادمة

**الترتيب المقترح:**
1. تنظيف الوثائق + إغلاق Coming Soon لـ support/disputes (أو إخفاء الروابط)  
2. تثبيت env الإنتاج (Stripe/جلسات) واختبار مسار شراء كامل يدويًا  
3. اختيار مسار Backend (API + DB) وترحيل المخازن الحرجة (مستخدمون، إعلانات، طلبات)  
4. إضافة Playwright smoke على المسارات الأساسية في CI  

بهذا ينتقل المشروع من **بيتا واجهة غنية ببيانات وهمية** إلى **منصة قابلة للتوسع التشغيلي**.

---

## 8) مراجع سريعة

| المورد | الرابط / المسار |
|--------|------------------|
| الإنتاج | https://sooqna.site |
| المستودع | https://github.com/dukkanify/UAE-Sales |
| الإدارة | https://sooqna.site/admin (يتطلب أدمن) |
| تقرير QA بالصور | `artifacts/qa/2026-08-03/تقرير-حل-الملاحظات.md` |
| تعليمات الـ Agent | `AGENTS.md` (يحتاج تحديث) |

---

*أُعدّ هذا التقرير وفق حالة `origin/main` بتاريخ 11 أغسطس 2026.*
