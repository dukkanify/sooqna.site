# Stripe (live mode keys from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_CURRENCY=aed

# Production canonical URL (required at build time for Stripe redirects)
NEXT_PUBLIC_APP_URL=https://sooqnauae.com

# Session cookie — share across apex and www (www redirects to apex)
SESSION_COOKIE_DOMAIN=.sooqnauae.com
# Required: HMAC signing for session cookies
SESSION_SECRET=

# Optional — external API
NEXT_PUBLIC_API_BASE_URL=

# Auth persistence — required. Vercel /tmp is ephemeral and must not store users.
# Neon, Vercel Postgres, or any Postgres URL. Existing users.json is imported once
# (insert-only; passwords of existing rows are never overwritten).
DATABASE_URL=postgres://USER:PASSWORD@HOST/DB?sslmode=require
# Vercel Neon integration also sets DATABASE_PGHOST, DATABASE_PGPASSWORD, etc.
# (auto-detected if DATABASE_URL is missing). Redeploy after adding env vars.
# POSTGRES_URL=

# Protect cron routes (dispute reminders). Send as Authorization: Bearer <secret>
CRON_SECRET=

# Feature flags
NEXT_PUBLIC_ENABLE_UAE_PASS=false
NEXT_PUBLIC_ENABLE_EMAIL_OTP=false
NEXT_PUBLIC_ENABLE_GUEST_CHECKOUT=true
# Must stay false in production — real Stripe only
NEXT_PUBLIC_ENABLE_MOCK_CHECKOUT=false
# Never set true on sooqnauae.com
ALLOW_MOCK_CHECKOUT=false

# Email / OTP (required for production OTP delivery)
# Vercel Resend integration must inject RESEND_API_KEY on Production (not Preview-only).
# Redeploy after adding. Do not use @resend.dev sender in production.
EMAIL_PROVIDER=resend
EMAIL_FROM_NAME=Sooqna
EMAIL_FROM_ADDRESS=no-reply@sooqnauae.com
RESEND_API_KEY=
OTP_EXPIRY_MINUTES=10
OTP_RESEND_COOLDOWN_SECONDS=60
OTP_PEPPER=
PASSWORD_PEPPER=
ENABLE_DEMO_OTP=false
NEXT_PUBLIC_ENABLE_DEMO_OTP=false

# Required at build/runtime for password-reset links and email CTAs
NEXT_PUBLIC_APP_URL=https://sooqnauae.com

# Web Push — set both, or rely on Postgres app_vapid_keys auto-provisioning
# npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
